template: "delay_pod_network_by_labels.yaml"
# random select a pod
podname: "$LEADER"
namespace: "$NAMESPACE"
localport: "$SERVERPORT"
# in milliseconds
latency: "10"
interface: "$IFACE"
# in milliseconds
duration: "20000"