apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata:
  name: time-shift
  namespace: chaos-testing
spec:
  mode: one
  mode: all
  selector:
    pods:
      $NAMESPACE: '$INJECTPODS'
  timeOffset: '$OFFSET'